#include <stdio.h>
#include <math.h>

struct Complex {
    float real;
    float imag;
};

float absolute(struct Complex c) {
    return sqrt((c.real * c.real) + (c.imag * c.imag));
}

float power_complex(struct Complex c) {
    return (c.real * c.real) + (c.imag * c.imag);
}